/**
 * One CV, named once. The hero button and the contact block both point here,
 * so the file and the saved filename can never drift apart.
 *
 * A single button is deliberate: a visitor choosing between four track-specific
 * CVs is a decision the site should make for them, not hand back.
 */
export const CV_PATH = '/resume_2026.pdf';

/* What the visitor sees in their downloads folder, rather than resume_2026. */
export const CV_DOWNLOAD_NAME = 'Sebastian-Chirinos-CV.pdf';
